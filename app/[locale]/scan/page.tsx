import type { Metadata } from "next"; import { BarcodeLookup } from "@/components/BarcodeLookup"; import { SiteHeader } from "@/components/SiteHeader"; import { pick } from "@/lib/i18n"; import { requireLocale } from "@/lib/locale-page"; import { getCatalog } from "@/lib/static-data"; type Props={params:Promise<{locale:string}>}; export const metadata:Metadata={robots:{index:false,follow:true}}; export default async function ScanPage({params}:Props){const locale=requireLocale((await params).locale);const products=getCatalog(locale).finderResults().map(({gtin,name,slug})=>({gtin,name,slug}));return <main><SiteHeader locale={locale}/><section className="subpage-hero compact-subpage-hero"><p className="eyebrow">{pick(locale,"Unterwegs entscheiden","Decide on the go")}</p><h1>{pick(locale,"Barcode scannen","Scan a barcode")}</h1><p>{pick(locale,"Scanne die Packung oder gib die Nummer ein. Kameraaufnahmen verlassen dein Gerät nicht.","Scan the package or enter the number. Camera images never leave your device.")}</p></section><section className="section"><BarcodeLookup locale={locale} products={products}/></section></main>}
