import type { Metadata } from "next"; import { PreferencesForm } from "@/components/PreferencesForm"; import { SiteHeader } from "@/components/SiteHeader"; import { pick } from "@/lib/i18n"; import { requireLocale } from "@/lib/locale-page"; type Props={params:Promise<{locale:string}>}; export const metadata:Metadata={robots:{index:false,follow:true}}; export default async function PreferencesPage({params}:Props){const locale=requireLocale((await params).locale);return <main><SiteHeader locale={locale}/><section className="subpage-hero compact-subpage-hero"><p className="eyebrow">{pick(locale,"Ohne Konto personalisieren","Personalize without an account")}</p><h1>{pick(locale,"Deine Standard-Präferenzen","Your default preferences")}</h1><p>{pick(locale,"Diese Einstellungen bleiben nur in deinem Browser.","These settings stay in your browser.")}</p></section><section className="section"><PreferencesForm locale={locale}/></section></main>}
