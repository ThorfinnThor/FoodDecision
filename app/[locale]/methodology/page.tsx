import type { Metadata } from "next";
import { SiteHeader } from "@/components/SiteHeader";
import { pick } from "@/lib/i18n";
import { localeAlternates, requireLocale } from "@/lib/locale-page";
type Props = { params: Promise<{ locale: string }> };
export async function generateMetadata({ params }: Props): Promise<Metadata> { const locale = requireLocale((await params).locale); return { title: pick(locale, "Methodik - Food Decision Engine", "Methodology - Food Decision Engine"), description: pick(locale, "So entstehen kategoriespezifische Scores und Empfehlungen.", "How category-specific scores and recommendations are created."), alternates: localeAlternates(locale, "/methodology") }; }
export default async function MethodologyPage({ params }: Props) { const locale = requireLocale((await params).locale); const c = (de: string, en: string) => pick(locale, de, en); return <main><SiteHeader locale={locale} /><section className="subpage-hero"><p className="eyebrow">{c("Transparent statt geheim", "Transparent by design")}</p><h1>{c("So kommen unsere Entscheidungen zustande", "How our decisions are made")}</h1><p>{c("Die Decision Engine nutzt feste, versionierte Regeln. Fehlende Angaben bleiben unbekannt und werden niemals als Null interpretiert.", "The Decision Engine uses fixed, versioned rules. Missing values remain unknown and are never treated as zero.")}</p></section><section className="section methodology-layout"><article><span>01</span><h2>{c("Kategorie zuerst", "Category first")}</h2><p>{c("Ein Haferdrink braucht andere Zielwerte als ein Proteinriegel. Bewertungen bleiben deshalb im Kontext der Produktgruppe.", "Oat milk needs different reference values than a protein bar, so every assessment stays within its category.")}</p></article><article><span>02</span><h2>{c("Mehrere Dimensionen", "Multiple dimensions")}</h2><p>{c("Nährwerte, Zutaten, Protein, Zucker, Familientauglichkeit und vegane Eignung bleiben getrennt sichtbar.", "Nutrition, ingredients, protein, sugar, family fit, and vegan suitability remain visible separately.")}</p></article><article><span>03</span><h2>{c("Unsicherheit zeigen", "Show uncertainty")}</h2><p>{c("Jeder Score trägt eine Datensicherheit. Datenlücken können die Veröffentlichung begrenzen.", "Every score includes a confidence level. Data gaps can limit publication.")}</p></article><article><span>04</span><h2>{c("Dein Ziel entscheidet", "Your goal matters")}</h2><p>{c("Der Finder gewichtet deine Priorität und wendet deine Ausschlüsse an.", "The finder weights your priority and applies your exclusions.")}</p></article></section><section className="detail-section"><div className="section-heading"><p className="eyebrow">{c("Grenzen", "Limits")}</p><h2>{c("Keine medizinische Beratung", "Not medical advice")}</h2><p>{c("Die Scores sind Entscheidungshilfen für vergleichbare Lebensmittel. Bei Allergien gilt immer die aktuelle Verpackung.", "Scores support comparisons between similar foods. For allergies, always rely on the current package label.")}</p></div></section></main>; }
