import type { Metadata } from "next"; import { FinderExperience } from "@/components/FinderExperience"; import { SiteHeader } from "@/components/SiteHeader"; import { pick } from "@/lib/i18n"; import { localeAlternates, requireLocale } from "@/lib/locale-page"; import { getCatalog } from "@/lib/static-data"; import type { ScoreType } from "@/lib/types";
const validGoals=new Set<ScoreType>(["overall_match","protein","low_sugar","vegan","family","ingredient_quality"]); type Props={params:Promise<{locale:string}>;searchParams:Promise<{goal?:string;q?:string}>};
export async function generateMetadata({params}:Props):Promise<Metadata>{const locale=requireLocale((await params).locale);return{title:pick(locale,"Produkt-Finder - Food Decision Engine","Product finder - Food Decision Engine"),description:pick(locale,"Finde Lebensmittel nach Kategorie, Ziel und Ausschlusskriterien.","Find foods by category, goal, and exclusions."),alternates:localeAlternates(locale,"/finder"),robots:{index:false,follow:true}}}
export default async function FinderPage({params,searchParams}:Props){const locale=requireLocale((await params).locale);const p=await searchParams;const goal=validGoals.has(p.goal as ScoreType)?p.goal as ScoreType:"overall_match";const catalog=getCatalog(locale);return <main><SiteHeader locale={locale}/><section className="subpage-hero finder-hero"><p className="eyebrow">{pick(locale,"Produkt-Finder","Product finder")}</p><h1>{pick(locale,"Was passt zu deinem Alltag?","What fits your everyday life?")}</h1><p>{pick(locale,"Wähle, was dir wichtig ist. Du bekommst eine nachvollziehbare Auswahl ohne Registrierung.","Choose what matters to you and get an explainable shortlist without an account.")}</p></section><FinderExperience categories={catalog.getCategories()} initialGoal={goal} initialQuery={p.q??""} locale={locale} products={catalog.finderResults()}/></main>}
